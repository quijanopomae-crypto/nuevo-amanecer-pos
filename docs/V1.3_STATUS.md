# Nuevo Amanecer POS — estado V1.3\n\n**Repository remediation:** `CODE_REMEDIATION_PASS / OWNER_FINALIZATION_PASS`.\n\nThis is not `REPOSITORY_CLEAN_PASS` and does not authorize production cutover. See `docs/REMEDIATION_FINAL_VERIFICATION.md`.

This document is the current human-readable status source for the V1.3 integration/remediation line on `feature/v1.3-mobile-cloud`.

## Release boundary

- The verified production baseline remains tag `v1.2-production`.
- V1.3 is an active integration/remediation line. It is not declared production cutover.
- No document, migration, LAB result, local Gate P result, or checkpoint commit authorizes a production deployment by itself.
- LAB D1 migrations, LAB Cloudflare deployment, real credential configuration, branch protection, and the real backup/recovery drill have been completed by the owner. Production cutover remains an explicit separate owner operation.

## Repository zones

- `POS/` is the CANON product tree.
- `laboratorio/pos-lab/` is UI/feature LAB only and cannot write CANON.
- `tools/cloudflare-lab/` is isolated Cloudflare/D1 LAB infrastructure.
- `tools/cloudflare-backup/` is backup/recovery tooling.
- `.opencode/` and `.agents/skills/` define the current agent/command/skill surface.
- `MANIFEST.yaml`, `orchestrator/`, and `infra/` belong to the explicit SHADOW/historical infrastructure path and do not gate normal product work unless SHADOW is deliberately invoked.

Use `REPO_MAP.yaml` for the compact machine-readable navigation map.

## Remediation state through Phase 7

Merged remediation has established:

- explicit sources of truth and CANON/LAB boundaries;
- safe CANON -> backup -> isolated LAB import contracts;
- LAB workspace recovery/idempotency and task scope guards;
- critical CANON CI with product, sync/outbox, backup and local migration gates;
- checkpoint classification instead of blind cherry-picking;
- backup/recovery code and dormant canonical commerce/financial schema preservation;
- separated CANON/LAB/SHADOW OpenCode roles;
- independent R2 read, LAB import HMAC and one-time POS activation secrets;
- persistent browser sessions with no hardware/device registration requirement;
- immutable SHA-pinned GitHub Actions;
- a GitHub Pages boundary where POS-LAB may use CANON static assets but the canonical `POS/index.html` application entry point is not served by Pages.

## Dormant V1.3 checkpoint material

The following are preserved for later integration but are not runtime authority:

- `tools/cloudflare-lab/migrations/0008_canonical_commerce.sql`
- `tools/cloudflare-lab/migrations/0009_canonical_financial.sql`
- `tools/cloudflare-lab/src/a6-commerce.js`
- `tools/cloudflare-lab/src/a6-financial.js`

There is no checkpoint activation migration in the current sequence, and these modules are not treated as an authorization to activate production commerce/financial runtime.

## Owner finalization completed

The owner-only repository controls are complete:

1. `LAB_IMPORT_HMAC_SECRET` and `POS_ACTIVATION_SECRET` were configured as real GitHub Actions secrets.
2. `feature/v1.3-mobile-cloud` branch protection was configured from the owner-authenticated GitHub CLI session.
3. LAB deployment run `36037787889` completed successfully, including D1 migrations, Worker deployment, secret binding, health verification and CANON baseline publication into the isolated LAB workspace.
4. Backup/recovery drill run `36040032934` completed successfully: production D1 export was read-only, SQL was validated, stored and re-read from private R2, restored into a temporary D1, reconciled with D1-compatible integrity checks, a PASS manifest was stored in R2, and the temporary D1 was deleted.

This closes `OWNER_ONLY_PENDING`. It does **not** authorize production cutover.

## Remaining repository remediation

- Phase 8: the current tree now contains the canonical status document, `REPO_MAP.yaml`, and a CI guard that verifies mapped paths and release boundaries.
- Phase 9: branch/history classification is complete. The checkpoint with 5 unique commits is preserved; 24 merged/recoverable refs are documented; demonstrated safe file deletions remain 0. See `docs/REMEDIATION_PHASE9_BRANCH_CLASSIFICATION.md`.
- Phase 10: final repository verification and guard are implemented in the current tree. OWNER_ONLY finalization is complete; production cutover remains separately gated and unauthorized.

## Historical and gate-specific documents

Documents named for V1.2, A2-A6 gates, remediation phases, evidence packs, snapshots, or freezes record the state and contract of that specific gate. They remain useful evidence, but they do not supersede this status document, `REPO_MAP.yaml`, `AGENTS.md`, or the current GitHub tree.

When historical text and the live repository differ, verify the live branch and current CI before acting.


## Operational owner finalizer

The owner finalization package at `tools/owner-finalize-v1.3.ps1` has been exercised. Device provisioning remains retired; the current model is one-time activation followed by a persistent session token.

Operational evidence:
- LAB deploy: `36037787889` — PASS.
- Backup/recovery drill: `36040032934` — PASS.
- Branch protection: configured from the owner-authenticated GitHub CLI session.
- Owner finalization state: `OWNER_FINALIZATION_PASS`.

The next work is not repository remediation. It is controlled V1.3 runtime/physical validation and, only after explicit authorization, production cutover.

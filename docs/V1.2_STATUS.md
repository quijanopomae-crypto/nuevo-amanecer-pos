# V1.2 — Estado verificable del cierre

Estado: **V1.2 PRODUCTION READY**. El E2E remoto, el visor publicado, la regresión
final y el arranque local están aprobados. El propietario confirmó la caja real con
sus datos y respaldo, y el visor desde un teléfono Android físico. El release se
identifica por el tag `v1.2-production`; el SHA del commit y del ZIP se registran en
`V1.2_RELEASE_REPORT.json`, junto al paquete final.

## Fuente y alcance

- Worktree: `C:\opus-worktrees\POS-WT-TURSO`.
- Rama: `feature/v1.2-cloudflare-cloud`.
- Base recibida: `7341dc79f6724bcb424a926ca00463e0b7d95c30`.
- Checkpoint V1.2-C: `c0b83e9`.
- Checkpoint inicial de este cierre: `642e90bde2c87fb13663061f506acfa24298080c`.
- Un PC escritor, una sola pestaña activa de escritura. V9 sigue activo y V10 dormante.
- Segundo dispositivo: ventas sincronizadas, sus líneas, movimientos y estado;
  exclusivamente lectura. No sincronización bidireccional ni carga automática del histórico.

## Cambios del cierre C

Los seis archivos previstos quedaron registrados en el checkpoint. La comprobación
de credenciales iguales ahora cierra lectura y escritura con 503, antes de acceder
a D1. CORS de `/read/*` anuncia solo GET, OPTIONS y x-read-token, incluidos errores.
OUTBOX, lógica financiera y V10 no fueron modificados.

## Despliegue comprobado

- Worker: `nuevo-amanecer-sync-lab`.
- URL: `https://nuevo-amanecer-sync-lab.nuevo-amanecer-pos.workers.dev`.
- Versión activa: `db6bcb98-59c1-48c6-97a8-dcabaafca6fe` (14), al 100%.
- Deployment: `dd7fed87-600d-4093-a93f-2457b2ba66c5`.
- El 401 del 11 de septiembre se corrigió activando la versión ya existente con los
  secretos renovados por el propietario. El tráfico seguía en la versión 12 mientras
  las actualizaciones estaban en las versiones 13/14 sin desplegar. Las versiones
  11 y 14 tienen el mismo ETag de código V1.2-C. No se cambiaron los tokens ni el Worker.
- Visor HTTPS: `https://nuevo-amanecer-sync-lab.nuevo-amanecer-pos.workers.dev/read-only.html`.
- D1: `nuevo-amanecer-lab`, ID `e734e6f1-41c4-4bfa-ab1f-5acbcdd2272e`.
- Migración `0002_read_only_indexes.sql`: aplicada; no se borraron ni recrearon tablas.
- El propietario actualizó SYNC_TOKEN en Cloudflare y `.dev.vars`; autenticación
  remota verificada. READ_TOKEN permanece distinto y carece de acceso de escritura.
- Copia histórica de READ_TOKEN: cifrada con Windows DPAPI bajo
  `%LOCALAPPDATA%\NuevoAmanecer\V1.2\READ_TOKEN.dpapi`; fuera del repositorio.
  Esa copia es anterior a la renovación del propietario y no acredita el token vigente.
  No usarla como fuente actual ni incluirla en entregables. No se modificó.

## Pruebas ejecutadas

| Grupo | PASS | FAIL | Evidencia |
|---|---:|---:|---|
| READ/CORS/OUTBOX focal | 25 | 0 | salida de ejecución; test nuevo de igualdad |
| Lectura del Worker remoto | 16 | 0 | `evidence/v1.2/remote-read.json` |
| RC Chrome, Worker real y SQLite local | 13 | 0 | `evidence/v1.2/rc-local.json` |
| POS, caja, créditos, inventario, catálogo, interfaz | 197 | 0 | `evidence/v1.2/pos-regression.tap` |
| OCR | 164 | 0 | `evidence/v1.2/ocr.tap` |
| Respaldo y restauración | 10 | 0 | `evidence/v1.2/backup-restore.tap` |
| Invariantes V9, escritura y V10 dormante | 48 | 0 | `evidence/v1.2/release-invariants.tap` |
| E2E Chrome contra Worker/D1 remoto | 7 | 0 | `evidence/v1.2/rc-remote.json` |
| Credenciales, operaciones exactas, retry y conflicto remotos | 17 | 0 | `evidence/v1.2/remote-release.json` |
| Publicación exclusiva de dos archivos, sin fuentes/secretos | 8 | 0 | `evidence/v1.2/reader-publication.json` |
| Visor HTTPS en Chrome a 390 px | 4 | 0 | `evidence/v1.2/hosted-reader.json` |
| Arranque local y restricciones del servidor | 1 | 0 | `tests/release-local-server.test.mjs`; salida de ejecución |
| Carpeta de entrega: configuración, arranque y OCR real | 6 | 0 | `evidence/v1.2/local-package-browser.json` |

La ejecución de cierre se conserva por separado en `evidence/v1.2/final-checks/`:
servidor local 1 PASS, navegador/configuración/OCR real 6 PASS e invariantes 48 PASS,
sin fallos ni omitidas. Se ejecutó sobre una copia de los archivos actuales, sin
alterar la caja comercial ni sobrescribir la evidencia de ejecuciones anteriores.

El RC local abre los archivos reales en Chrome con un perfil aislado. Incluye
ventas online/offline, cierre completo y reapertura offline, recarga, idempotencia,
errores 401/409/503, red caída, acuse perdido, recuperación y respaldo sin tokens.
El visor se comprobó en otro perfil a 390 px. Eso **no es un segundo dispositivo físico**.
El RC remoto del 11 de septiembre creó dos ventas identificadas como
`PRUEBA RC V1.2 — NO COMERCIAL` en un perfil aislado. Tras una venta sin internet,
cierre completo, reapertura offline y reconexión, D1 recibió seis operaciones:
dos ventas, dos líneas y dos movimientos. La consulta SQL directa en
`evidence/v1.2/d1-remote-rc.json` coincide con sus seis UUID, sin duplicados.
Dispositivo del ensayo: `092c7c33-8efd-4943-a63d-f7263156edd6`; ventas V-001 y V-002.
El reintento conservó una sola operación y un payload distinto recibió 409,
manteniendo el registro original. Las rutas de lectura mostraron los mismos datos.

El build de publicación copia exclusivamente el HTML y JavaScript canónicos del
visor. Sus bytes publicados coinciden con POS. Las rutas del POS escritor,
configuración, fuentes del Worker y `.dev.vars` devuelven 404. No se cambió
`worker.js`, OUTBOX, la interfaz del POS principal ni reglas financieras.

Después del E2E y la publicación se repitieron 197 pruebas POS, 164 OCR, 10 de
respaldo, 25 cloud y 48 invariantes: todas aprobadas, sin omitidas. La suite local
de 13 escenarios de fallos conserva su evidencia anterior; no fue reabierta.

La primera ejecución de invariantes tuvo 47 PASS/1 FAIL: G1.1 esperaba una única
aparición de la cadena FIFO en todo el archivo. V1.2-B añadió otra para los acuses.
Se corrigió exclusivamente la prueba para exigir la misma cadena en cada función;
las 48 pruebas pasaron después. La salida anterior se conserva. Las evidencias
históricas originales no se reemplazaron; los resultados actuales se copiaron a
`evidence/v1.2/release-invariants/`. Sus campos base/rama históricos proceden del
harness anterior; esta ejecución corresponde al worktree y checkpoint indicados arriba.

Tras corregir la activación de secretos se repitieron las 16 pruebas READ y las cuatro
del visor HTTPS, todas aprobadas. `read-auth-deployment.json` registra la versión activa,
los nombres de bindings y comprobaciones de autenticación sin valores secretos.

La carpeta de entrega se abrió por HTTP local en un contexto de navegador nuevo. Se
comprobó rechazo de clave incorrecta, persistencia de la clave válida, preservación ante
un reemplazo inválido, arranque del POS y reconocimiento OCR real con Worker/WASM/español
incluidos. No hubo escrituras cloud ni archivos locales ausentes. Esto no configura el
perfil comercial del propietario ni sustituye un respaldo de sus datos.

## Confirmación del propietario

`READ_ONLY_REAL_DEVICE=PASS` y `BLOCKER=NONE`.

El propietario confirmó en un teléfono Android físico la autenticación READ_TOKEN,
el mensaje «Consulta actualizada.», Estado, Ventas, Ver líneas, Inventario y ausencia
de escritura. Contadores observados: dos ventas, dos líneas y dos movimientos.
La confirmación está registrada en `evidence/v1.2/operational-acceptance.json`.

También confirmó el POS real en `http://127.0.0.1:8788/POS/index.html`, sus datos
comerciales presentes, el funcionamiento de `INICIAR_POS.cmd`, la configuración con
«Conexión verificada y guardada.» y el respaldo completo
`nuevo_amanecer_backup_completo_2026-09-11.json`. Estas comprobaciones físicas y de
datos reales son declaraciones explícitas del propietario, no pruebas simuladas.
No se encontraron P0/P1/P2 en el alcance auditado.

## Entrega y recuperación

El iniciador `INICIAR_POS.cmd` y `tools/pos-local/server.mjs` preparan una dirección
local estable para utilizar también OCR. No cambian archivos POS, OUTBOX ni finanzas.
La página auxiliar `tools/pos-local/setup.html` permite verificar y guardar la credencial
de escritura desde el perfil elegido mediante la API existente.
La caja real, la credencial guardada y el respaldo comercial fueron confirmados por
el propietario. `.dev.vars` no configura automáticamente un perfil nuevo. Ver
`docs/V1.2_USO.md` para preparar otro perfil durante una recuperación.

El paquete `Nuevo_Amanecer_POS_V1.2_Production.zip` se construye exclusivamente con
los archivos permitidos en `evidence/v1.2/production-package-files.json`, tomados del
commit identificado por `v1.2-production`. Su verificación de contenido, tamaño,
SHA-256 y copia recuperable se registra fuera del ZIP en `V1.2_RELEASE_REPORT.json`
y en el archivo `.sha256` acompañante. La preparación anterior se conserva en
`evidence/v1.2/package-preparation.json` como evidencia histórica, no como reporte final.

El tag no se sobrescribe. Se excluyen del commit y del paquete los cambios personales;
se excluyen del paquete también las dependencias de desarrollo y los perfiles. Excluir siempre
`opencode.json`, `tools/turso-lab/**`, `nul`, `.dev.vars`, `.env*`, `.wrangler`,
`node_modules`, `.reader-assets` generado y perfiles del navegador. La auditoría final
está en `evidence/v1.2/final-release-audit.json`; los manifiestos anteriores conservan
sus fechas originales para distinguirlos del cierre definitivo.

---
name: dependency-map
description: "Representa módulos y dependencias de lectura, escritura, globals, storage, DOM y pruebas."
---

# Dependency Map

Mapea `module`, `depends_on`, `read_dependencies`, `write_dependencies`, `shared_globals`, `storage`, `DOM` y `tests`. No analiza automáticamente el HTML completo.
